# The desktop package must own its complete dependency tree. In particular,
# Electron rebuilds better-sqlite3 for its ABI, so a file: dependency must be
# packed here instead of symlinked to the repository root.
install-links=true
