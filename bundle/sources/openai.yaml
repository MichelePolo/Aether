# Codex harness config for the skill-smith skill. Read by the product, not by
# the model — the instructions live in SKILL.md.
# Schema: https://developers.openai.com/codex/skills
interface:
  display_name: "Skill Smith"
  short_description: "Author, audit and package Agent Skills"
  default_prompt: "Use $skill-smith to turn this workflow into a reusable skill."
policy:
  allow_implicit_invocation: true
